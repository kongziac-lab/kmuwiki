# 검색·RAG 서비스(Vercel) + 공유 코어 — 가벼운 런타임 의존성만.
#   로컬 인제스트(문서 파서 포함)는 requirements-worker.txt 사용.
supabase>=2.6
python-dotenv>=1.0     # .env 자동 로드(로컬). Vercel은 프로젝트 env 사용
cohere>=5.0            # 임베딩(embed-multilingual-v3.0) + chat(command-r)
fastapi>=0.117         # 검색·RAG API (Vercel ASGI 자동 인식)

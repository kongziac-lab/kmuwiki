# 로컬 인제스트 워커 — 코어 + 문서 파서 + 로컬 서버 실행.
#   설치: pip install -r requirements-worker.txt
-r requirements.txt

# 문서 파싱
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1
xlrd>=2.0              # 구형 .xls
olefile>=0.47          # HWP 잠금 탐지 + PrvText 추출

# 로컬에서 검색 서비스 직접 실행 시
uvicorn>=0.30

# 선택(주석 해제 시 활성화):
# transformers>=4.44   # §7.A NER(이름·주소 마스킹) — 정책에서 켤 때만
# torch>=2.2
# easyocr>=1.7         # 1차 OCR(CPU)
# paddleocr>=2.7       # 2차 OCR(GPU 서버)
# FlagEmbedding>=1.2   # 로컬 임베딩(bge-m3)
# anthropic>=0.40      # LLM을 Claude로 쓸 때
